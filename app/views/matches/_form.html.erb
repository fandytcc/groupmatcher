<%= form_for @match do |f| %>

  <div class="form-group">
    <label>Date:</label>
    <%= f.date_select :day, prompt: "Select a date", class: "form-control" %>
  </div>

  <div class="actions">
    <%= f.submit "SUBMIT", class: "btn btn-default" %>
  </div>
<% end %>
